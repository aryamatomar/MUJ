# Icons Directory

This folder contains SVG and icon assets for the SafeHer Women Safety IoT interface.
Icons are also embedded as clean, self-contained inline SVGs in `index.html` to guarantee zero external dependency and full offline capability when opening directly with a browser.
